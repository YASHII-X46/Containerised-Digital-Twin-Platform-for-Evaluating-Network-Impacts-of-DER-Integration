# NATS server — the OpenFMB message bus for the DT stack (native NATS, 4222).

http_port: 8222          # monitoring/health (http://broker:8222)

# Profiles and result series travel over the bus as one message each, so allow
# payloads well above the 1 MB default.
max_payload: 16MB
